apiVersion: v1
kind: Deployment
metadata:
  name: rainchasers-nrw
spec:
  replicas: 1
  selector:
    matchLabels:
      name: rainchasers-nrw
  template:
    metadata:
      labels:
        name: rainchasers-nrw
    spec:
      containers:
        - name: rainchasers-nrw
          image: eu.gcr.io/tuleyprod/gauge/nrw:latest
          env:
            - name: PROJECT_ID
              value: tuleyprod
            - name: PUBSUB_TOPIC
              value: com.rainchasers.gauge
            - name: NRW_API_KEY
              valueFrom:
                secretKeyRef:
                  name: nrw-apikey
                  key: key
            - name: HONEYCOMB_API_KEY
              valueFrom:
                secretKeyRef:
                  name: honeycomb-writekey
                  key: key
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
